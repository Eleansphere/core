{
  "name": "@eleansphere/entity-core",
  "version": "1.0.0",
  "description": "Define an entity once (fields + validation) and derive its be-core ModelConfig, DTO classes, and service-core service class from it",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Eleansphere/entity-core.git"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "release": "npm run build && changeset publish"
  },
  "keywords": [],
  "author": "Eleansphere",
  "license": "ISC",
  "dependencies": {
    "@eleansphere/service-core": "^1.5.1"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "@eleansphere/be-core": "^1.10.0",
    "typescript": "^5.4.5"
  }
}
