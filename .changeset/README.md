# Changesets

One `.changeset/*.md` file per shippable change. It names the affected packages and their bump size
(`patch` / `minor` / `major`) and describes the change for consumers. Create one with
`pnpm changeset`. See [CONTRIBUTING.md](../CONTRIBUTING.md) for the release flow.
